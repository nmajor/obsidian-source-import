#!/usr/bin/env bash
__dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

echo "Enter the path to the vault you want to use:"
read -r VAULT_PATH

echo "Setting vault path to $VAULT_PATH"

echo $VAULT_PATH > "$__dir"/.vault-path

echo "Set vault path to $VAULT_PATH"

